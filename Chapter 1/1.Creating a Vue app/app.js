const app = Vue.createApp({         // We set an object inside -> could be data, functions
//template: '<h2>I am the template</h2>' // So anything inside here, will replace the DOM element inside the #app div
})

app.mount('#app') // Will be displayed as a component inside the #app div
